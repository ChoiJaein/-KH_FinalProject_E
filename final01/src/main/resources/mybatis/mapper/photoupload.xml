<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="photoUploadMapper">
<!--
 	<select id="getFileIdSeq" resultType="_int">
		SELECT FILEUPLOAD_SEQ.NEXTVAL FROM DUAL
	</select>
	<insert id="insertData" parameterType="photoUploadDto">
		INSERT INTO 
		PHOTOUPLOAD(FILEID
				  , FILENAME
				  , UUIDNAME
				  , LOCATION
				  , URL
				  , FILESIZE
				  , FILETYPE)
		     VALUES(#{fileid}
		     	  , #{filename}
		          , #{uuidname}
		          , #{location}
		          , #{url}
		          , #{filesize}
		          , #{filetype})
	</insert>
	
	<select id="selectDatas" resultType="photoUploadDto">
		SELECT *
		  FROM PHOTOUPLOAD
		 WHERE ACCOUNTID = #{accountid}
	</select> 
-->
</mapper>