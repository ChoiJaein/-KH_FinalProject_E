<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberMapper">
	<insert id="register" parameterType="memberVo">
		INSERT INTO 
		ACCOUNT(ACCOUNTID
			  , PASSWORD
			  , NAME
			  , EMAIL
			  , PHONE
			  , ADDRESS)
		VALUES(#{accountid}
			 , #{password}
			 , #{name}
			 , #{email}
			 , #{phone}
			 , #{address})
	</insert>
	
	<!-- 아이디 중복 확인 -->
	<select id="idOverlap" parameterType="memberVo" resultType="memberVo">
		SELECT ACCOUNTID FROM ACCOUNT WHERE ACCOUNTID = #{accountid}
	</select>
</mapper>