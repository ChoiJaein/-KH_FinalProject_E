<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardMapper">
	<select id="getNextSeq" resultType="_int">
		SELECT EMP_BOARDS_SEQ.NEXTVAL FROM DUAL
	</select>
	<insert id="insertData" parameterType="boardDto">
		INSERT INTO BOARD(BID, CATEGORYID, BTITLE,
						  BCONTENT, DEALMETHOD, PRICE,
						  PCONDITION, CREATEDATE)
		     VALUES(#{bid}, #{categoryid}, #{btitle}, 
		     		#{bcontent}, #{dealmethod}, #{price}, 
		     		#{pcondition}, SYSDATE)
	</insert>
	

</mapper>